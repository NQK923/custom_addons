<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Main menu action -->
    <record id="action_healthcare_dashboard" model="ir.actions.act_url">
        <field name="name">Quản lý chăm sóc khách hàng y tế</field>
        <field name="url">/healthcare/dashboard</field>
        <field name="target">self</field>
    </record>

    <!-- Add to website menu -->
    <record id="website_menu_healthcare" model="website.menu">
        <field name="name">Chăm sóc KH Y tế</field>
        <field name="url">/healthcare/dashboard</field>
        <field name="parent_id" ref="website.main_menu"/>
        <field name="sequence" type="int">50</field>
    </record>
</odoo>